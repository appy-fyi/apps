TODO:
    InkSend:
        handwritten font:
            - When drawing a glyph it is impossible to just draw dot by just touching the screen, fix this.
Done:
    InkSend:
        - When creating handwritten font, the glyph disappears once I removed my finger from the screen, fix this.
            Also, when done writing a glyph, there should be button "Save" at the same screen, when pressing on it move to the next glyph without a confirmation.
        - On the creation screen make a grid of all the glyphs that need to be drawn, mark the ones that are already drawn, and allow the user to click on any of them to edit it.
            Show a small preview of the glyph in the grid, if the  user already drew it show the preview of the drawn glyph, if not show a placeholder.

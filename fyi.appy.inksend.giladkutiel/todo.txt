TODO:
    Color Picker:
        - Make a nice color picker to replace the input field, you can limit the selection to a small set of predefined colors.
    Languages:
        - Add support for multiple languages, add English by default and let users select additional languages. 
            Better yet if the user's languages can be detected automatically add them.
DONE:
    Font selection:
        Font selection layout is broken, please take a look and fix.
    Image Rendering:
        - Please make sure text is not midword broken, i.e. it wraps only at spaces and not in the middle of words.
    Emoji Selector:
        Make an emoji selector for the emoji not an input field, you can limit the selection to a small set of predefined popular and diverse emojis.

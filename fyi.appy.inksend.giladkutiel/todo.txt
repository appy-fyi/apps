TODO:
    - Style:
        - Associate an emoji with each style, render the emoji in the image, if possible use vector font for the emoji so it is rendered correctly.

DONE:
    Image:
        make it always 512x512, full square, no border radius, text align center both vertically and horizontally, adust font size to fill the square.

        remove the font size from the settings and make it automatic based on the text length.

    Overflow Buttons:
        If possible show the buttons only when the user entered text in the whatsapp input field.

        make two Overflow buttons with two different styles.

    Rebuild InkSend:
        Ignore all files other than inksend.md, in particular ignore the com.takeofflabs.fontmaker-build-spec.json file and ignore all existing code.
        You task is to build from scratch a new version of InkSend, you need to:
        1. Read `inksend.md` and understand the requirements and specifications for InkSend.
        2. Update the com.takeofflabs.fontmaker-build-spec.json file to reflect the new build specifications for InkSend.
        3. Build the first version of InkSend and push it to my phone.
        4. Wait for my feedback before proceeding to the next version.

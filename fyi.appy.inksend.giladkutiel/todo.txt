TODO:
    Languages: 
        - UI interface is just English.
        - Maintain a font list for 5 popular languages + Hebrew (see below)
    Image Generation Pipeline:
        For image generation we use sentiment analysis + keyword lookup
        Sentiment analysis determines the colors and font family, while the keyword lookup determines the emojis.
        Pipeline:
            - Translate to English (if not already)
            - Run sentiment analysis (Media Pipe on device) and output (positive, negative, neutral)
            - For each sentiment predefined at least 3 font families for each language
            - For each sentiment predefined at least 6 color gradients (common to all languages)
            - Given a sentiment select a random gradient and a random font family (independently) for the language 
            - tokenize the English sentence and use built in steamer to extract keywords
            - pre define a dictionary of keywords to emojis, make sure we have at least 100 popular diverse emojis
            - Generate the image:
                - Text is on the top and naturally aligned to the start (left for LTR languages, right for RTL languages)
                - emojis at the bottom centered horizontally, with a max of 3 emojis
                - Font size should be set automatically to fill the space of the image, no word breaks, no text overflow, and no text cut off. 
    Button:
        - Make a loading indicator for the button when busy (e.g. translating, downloading language model, etc...)
DONE:
    Auto Style:
        Do not keep a dictionary per language, always translate the text to English then use the English dictionary to select the style.
        Make sure fonts for the other languages are supported and look good.
        Make sure languages that are not English also work
        Always use color gradient for the background.
        Horizontal center the emoji.
        Change the style of the button, use the ink emoji (🫟) as the default icon with light background, make sure the button is a perfect circle.
        If possible update the color and emoji based on the typed text, if too much compute is required, then just use the default style.
        Remove the predefined styles and the options to add and remove styles.
        Instead we generate a style based on the content of the text.
        How it works:
            - Generate a predefined set of 5 - 15 intents (e.g. "funny", "sad", "romantic", "angry", "informative", etc...) and store them in a static dictionary.
            - For each intent generate 3 - 5 matching styles (font family, emoji, text color, background color)
            - Create a static dictionary that maps keywords to intents (e.g. "funny" -> intent 1, "sad" -> intent 2, "romantic" -> intent 3, etc...)
            - Given the user's text, translate it to English, then lookup the keywords in the dictionary and select the most relevant intent, then pick a random style.
            - Create a default intent and styles for texts that don't match any intent.

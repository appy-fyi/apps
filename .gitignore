# Secrets and credentials -- never commit these, in the root or in any app subfolder
.env
.env.*
service-account.json
*.jks
*.keystore
keystore.properties

# Claude Code session-local ephemera (per app subfolder)
**/.claude/RESUME.md
**/.claude/scheduled_tasks.lock

# Android/Gradle build artifacts (per app subfolder)
*.iml
.gradle/
.kotlin/
**/local.properties
.idea/
.DS_Store
**/build/
**/captures/
.externalNativeBuild
.cxx
